package utils

object prueba {

/*
def buscar(lista: List[Int], com:(Int, Int) => Boolean): Int =
      if (lista.tail.isEmpty) lista.head
      else if (com(lista.head, buscar(lista.tail,com))) lista.head
      else  buscar(lista.tail,com)

def mayor(lista: List[Int]):Int = buscar(lista, (a:Int, b:Int) => (a>b))

def menor(lista: List[Int]):Int = buscar(lista, (a:Int, b:Int) => (a<b))
mayor(List(1,2,3,4,5,6))
menor(List(1,2,3,4,5,6))
buscar(List(1,2,4,3,5,6), (a:Int, b:Int) => (a==4))

def existe(lista: List[Int], a: Int): Boolean = buscar(lista, (x:Int, y:Int) => (a==x)) == a

existe(List(1,2,3,4,5,6),5)
existe(List(1,2,3,4,5,6),50)
*/

}
