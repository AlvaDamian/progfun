package utils

object temp {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(65); val res$0 = 
  (new Fraccion(2, 4)).simplificar();System.out.println("""res0: utils.Fraccion = """ + $show(res$0))}
}
